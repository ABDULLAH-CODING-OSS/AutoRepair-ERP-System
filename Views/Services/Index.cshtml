@model IEnumerable<AutoRepairERD.Models.Service>

@{
    ViewData["Title"] = "Services";
}

<div class="container py-4">
  <div class="d-flex justify-content-between align-items-center mb-3">
    <h1>Services</h1>
    <a asp-action="Create" class="btn btn-primary">Create New</a>
  </div>

  <div class="card">
    <div class="card-body p-0">
      <table class="table mb-0 table-striped">
        <thead class="table-light">
          <tr>
            <th>@Html.DisplayNameFor(m => m.ServiceName)</th>
            <th>@Html.DisplayNameFor(m => m.Description)</th>
            <th>@Html.DisplayNameFor(m => m.StandardHours)</th>
            <th>@Html.DisplayNameFor(m => m.FixedPrice)</th>
            <th>@Html.DisplayNameFor(m => m.IsActive)</th>
            <th class="text-end">Actions</th>
          </tr>
        </thead>
        <tbody>
        @foreach (var item in Model) {
          <tr>
            <td>@Html.DisplayFor(_ => item.ServiceName)</td>
            <td>@Html.DisplayFor(_ => item.Description)</td>
            <td>@Html.DisplayFor(_ => item.StandardHours)</td>
            <td>@Html.DisplayFor(_ => item.FixedPrice)</td>
            <td>@Html.DisplayFor(_ => item.IsActive)</td>
            <td class="text-end">
              <a asp-action="Edit" asp-route-id="@item.ServiceId" class="btn btn-sm btn-outline-primary me-1">Edit</a>
              <a asp-action="Details" asp-route-id="@item.ServiceId" class="btn btn-sm btn-outline-secondary me-1">Details</a>
              <a asp-action="Delete" asp-route-id="@item.ServiceId" class="btn btn-sm btn-outline-danger">Delete</a>
            </td>
          </tr>
        }
        </tbody>
      </table>
    </div>
  </div>
</div>
