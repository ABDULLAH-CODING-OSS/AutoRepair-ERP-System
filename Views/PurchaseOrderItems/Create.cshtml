@model AutoRepairERD.Models.PurchaseOrderItem

@{
    ViewData["Title"] = "Add Purchase Order Item";
}

<div class="card">
    <div style="padding:4px 0">
        <h4 class="card-title">Add Item</h4>

        <form asp-action="Create" method="post">
            @Html.AntiForgeryToken()
            <div asp-validation-summary="All" class="text-danger mb-3"></div>

            <div style="margin-bottom:12px">
                <label class="form-label">Purchase Order</label>
                <select asp-for="PurchaseOrderId" class="form-control" asp-items="ViewBag.PurchaseOrders">
                    <option value="">-- Select Purchase Order --</option>
                </select>
                <span asp-validation-for="PurchaseOrderId" style="color:var(--red)"></span>
            </div>

            <div style="margin-bottom:12px">
                <label class="form-label">Part</label>
                <select asp-for="PartId" class="form-control" asp-items="ViewBag.Parts">
                    <option value="">-- Select Part --</option>
                </select>
                <span asp-validation-for="PartId" style="color:var(--red)"></span>
            </div>

            <div style="margin-bottom:12px">
                <label asp-for="Quantity" class="form-label"></label>
                <input asp-for="Quantity" class="form-control" />
                <span asp-validation-for="Quantity" style="color:var(--red)"></span>
            </div>

            @* Unit cost and line total are calculated on the server and are not shown on the create form. *@

            <div style="margin-bottom:12px">
                <input type="submit" value="Add Item" class="btn-gold btn-sm" />
                <a asp-action="Index" class="btn btn-secondary ms-2">Cancel</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
