@model AutoRepairERD.Models.Notification

@{
    ViewData["Title"] = "Notification Details";
}

<div class="card">
    <div style="padding:4px 0">
        <h5 class="card-title">@Model.Title</h5>
        <p class="td-muted">Type: <span class="badge badge-neutral">@Model.NotificationType</span></p>
        <p>@Model.Message</p>
        <dl class="row">
            <dt class="col-sm-3">Recipient</dt>
            <dd class="col-sm-9">@(Model.User != null ? Model.User.FullName ?? Model.User.Username : "System")</dd>

            <dt class="col-sm-3">Created On</dt>
            <dd class="col-sm-9">@Model.CreatedAt?.ToString("yyyy-MM-dd HH:mm")</dd>

            <dt class="col-sm-3">Status</dt>
            <dd class="col-sm-9">
                @if (Model.IsRead == true)
                {
                    <span class="badge badge-neutral">Read</span>
                }
                else
                {
                    <span class="badge badge-ordered">Unread</span>
                }
            </dd>
        </dl>

        <div style="margin-top:12px">
            <a asp-action="Index" class="btn-outline btn-sm">Back to List</a>
        </div>
    </div>
</div>
